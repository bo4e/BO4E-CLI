{%- if missing %}
// Default impl omitted: {% if missing | length > 1 %}fields{% else %}field{% endif %} `{{ missing | join("`, `") }}` {% if missing | length > 1 %}have{% else %}has{% endif %} no default expression.
{%- else %}
impl Default for {{ class_name }} {
    fn default() -> Self {
        Self {
{%- for f in fields %}
            {{ f.name }}: {{ f.expr }},
{%- endfor %}
        }
    }
}
{%- endif %}
