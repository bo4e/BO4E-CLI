//! BO4E {{ version }} — generated Rust types.
//!
//! Querying the BO4E version at runtime: `crate::VERSION`.

{% for m in top_modules -%}
pub mod {{ m }};
{% endfor %}
pub const VERSION: &str = "{{ version }}";

/// Default value for `_version` fields on generated models.
/// Used by serde's `default = "..."` attribute.
pub(crate) fn default_version() -> String {
    VERSION.to_string()
}
